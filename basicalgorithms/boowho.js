function booWho(bool) {
    // What is the new fad diet for ghost developers? The Boolean.
    return typeof bool === "boolean"; //typeof operator returns a string indicating the type of unevaluated operand.
  }
  
  booWho(null);